
        <div class="postBoxContainer">
            <div class="postBox card">
                <div class="userBox">
                    <h4 class="userName">{{user.username}}:</h4>
                </div>
                <div class="card post1" >
                    <h2 id="postTitle">{{title}}</h2>
                    <h4 id="postContent">{{content}}</h4>
                </div>

                <div class="commentLink">
                    <a href="/post/{{id}}" class="commentTag2" style="text-decoration:none;"></a>
                </div>

            </div>

        </div>

        <div class="commentBoxContainer">
            <div class="commentBox card">
                <form class="form newComment-form">
                    <div class="input-group  mb-3">
                        <label class="input-group-text" for="commentText">Add Comment:</label>
                        <input  class="form-control"  type="text" id="commentText" />
                    </div>

                    <div class="form-group commentBtn">
                        <button class="btn btn-primary" type="submit">Add</button>
                    </div>
                </form>
            </div>
        </div>